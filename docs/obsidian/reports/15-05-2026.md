# Daily Report - 15-05-2026

## Features
- Checklist and high-priority assessment metrics now show DONE progress as `done / total` on the home dashboard and playground output panel.
- Added `done_items` and `high_priority_done_items` to `AssessmentSummary` (backend and frontend) so DONE counts stay in sync when checklist status is updated.

## Improvements
- `get_latest_assessment` recomputes summary from live checklist items via `build_assessment_summary` instead of stale row counters.
- Shared `formatChecklistItemsMetricValue` and `formatHighPriorityMetricValue` helpers in `frontend/src/lib/assessment-metrics.ts`.

## Fixes
- None.
