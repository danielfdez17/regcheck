# Daily Report - 19-05-2026

## Features

- Added `PATCH /api/v1/gdpr/assessments/{assessment_id}` to regenerate an existing assessment snapshot while preserving checklist status and evidence for unchanged item IDs.
- Added `GET /api/v1/gdpr/assessments/{assessment_id}/export?format=csv|markdown` for server-side checklist exports.
- Wired frontend save flow to call PATCH when editing a persisted assessment instead of always creating a new snapshot.
- Added CSV and Markdown export actions in the GDPR playground sidebar.

## Improvements

- Updated `docs/obsidian/PROJECT-REPORT.md` API reference and MVP export status for the new routes.

## Verification

- `make pylint` on touched backend modules.
- `make typecheck` and `make lint` on the frontend.
