# Daily Report - 18-05-2026

# Dashboard
## Features
- Added an authenticated assessment dashboard shown after login instead of opening the latest audit directly.
- Added saved assessment summaries with progress, selected rule counts, and explicit view/edit actions.
- Added an API endpoint to load a specific persisted assessment by id for the current user.

## Improvements
- Increased assessment history retrieval to support a larger dashboard list.
- Reused the existing GDPR playground as the editor for both new assessment drafts and selected historical assessments.
- Kept dashboard summaries synchronized when checklist statuses or evidence metadata are updated.

## Fixes
- Users with multiple assessments can now reopen older assessments instead of being forced into the latest one.
- Assessment loading remains scoped to the authenticated owner before returning any saved audit detail.
- Hid selected-rule, checklist, high-priority, and assessment-history summary cards until an assessment is opened in the editor.
- Removed the duplicate assessment metric summary from the `ASSESSMENT OUTPUT` section.

## Verification
- `make typecheck` passed.
- `make lint` passed.
- `.venv/bin/python -m compileall backend/app/gdpr backend/app/api/v1/gdpr.py` passed.
- `.venv/bin/python -m pylint backend/app/api/v1/gdpr.py backend/app/gdpr/assessments.py backend/app/gdpr/schemas.py` passed.

---
# Checklist items

## Features
- Added a new `SavedEvidenceUrlRow` component to display evidence metadata for checklist items.

## Improvements
- Added a reusable frontend evidence URL helper to keep save-time normalization and render-time link behavior consistent.

## Verification
- `pnpm --dir frontend run typecheck` passed.
- `pnpm --dir frontend run lint` passed.
- IDE diagnostics found no linter errors in the touched frontend files.

## Fixes
- Ensured saved evidence links use absolute URLs so entries like `example.com` open as external links instead of routing inside the app.
- Normalized newly saved evidence URLs to include an explicit `https://` scheme when users omit one.
- Fixed `make dev-backend` startup by allowing backend settings to ignore shared non-`REGCHECK_` `.env` keys.
- Updated the local backend target to use a SQLite database by default instead of the Docker-only `postgres` hostname.
- Fixed local login 404 errors by treating a blank `VITE_API_BASE_URL` as unset and falling back to the backend URL.

## Improvements
- Added a reusable frontend evidence URL helper to keep save-time normalization and render-time link behavior consistent.

## Verification
- `pnpm --dir frontend run typecheck` passed.
- `pnpm --dir frontend run lint` passed.
- IDE diagnostics found no linter errors in the touched frontend files.
- `timeout 8s make dev-backend` reached `Application startup complete`.
- `.venv/bin/python -m compileall backend/app/core backend/app/main.py backend/app/db/session.py` passed.
- `pnpm --dir frontend run typecheck` passed.
- `pnpm --dir frontend run lint` passed.
