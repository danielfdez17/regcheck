# Daily Report - 16-05-2026

## Features
- Added strong password policy enforcement for new account signup.
- Passwords must now be 12-128 characters and include uppercase, lowercase, number, and symbol characters with no spaces.
- Added live signup password requirement feedback in the authentication form.

## Improvements
- Centralized backend password validation in `backend/app/auth/password_policy.py`.
- Mirrored password policy constants and checks in `frontend/src/auth/password-policy.ts` for immediate client-side feedback.
- Updated English and Spanish auth translations with password policy guidance.
- Improved API validation error parsing so backend password policy errors are shown clearly in the frontend.
- Added assessment creator ownership metadata to persisted GDPR assessments.
- Added a startup schema guard that backfills the new assessment ownership column for existing SQLite databases.

## Fixes
- Replaced the previous weak signup constraint that only required an 8-character minimum password.
- Scoped assessment history, latest assessment retrieval, and checklist updates to the authenticated creator instead of the whole tenant.
- Stopped the latest-assessment load from implicitly creating a persisted assessment for users with no history.

## Verification
- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `python3 -m compileall app/auth` passed.
- Earlier backend `pylint` verification could not run because `pylint` was not installed in that active Python environment.
- `python3 -m compileall backend/app` passed.
- `pnpm --dir frontend run typecheck` passed.
- `pnpm --dir frontend run lint` passed.
- `.venv/bin/python -m pylint backend/app/api/v1/gdpr.py backend/app/db/models.py backend/app/db/session.py backend/app/gdpr/assessments.py` passed.
- Temporary SQLite schema smoke test passed for adding `created_by_user_id` to an existing `compliance_assessments` table.
