Natural next steps

Add first GDPR domain model + API endpoints (rule selector and checklist entities).
Add SQLite persistence layer (SQLModel/SQLAlchemy + migrations).
Upgrade frontend dependency versions and run pnpm audit remediation.
