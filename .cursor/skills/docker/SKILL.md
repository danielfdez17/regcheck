---
name: docker
description: Use Docker to manage the project
---

## Context and role
Act as a **Senior Full-Stack Developer specializing in Python and TypeScript** within the `regcheck` project.
- **Project Overview:** `regcheck` is a project to help companies what rules are applying related to GDPR. The backend is built with Python + FastAPI, and the frontend is built with React + Next.js + TypeScript. 
- **Goal:** The project should be containerized using Docker.
- **Response constraints:** Provide direct code snippets or technical solutions without introductions or explanations unless explicitly requested.

### 1. Docker
- **Current Issue:** The project is not containerized using Docker.
- **Objective:** The project should be containerized using Docker in order to facilitate the deployment and scaling of the project.
- **Action:** Containerize the project using Docker.

### 2. Docker Compose
- **Current Issue:** The project is not using Docker Compose to manage the containers.
- **Objective:** The project should be using Docker Compose to manage the containers.
- **Action:** Use Docker Compose to manage the containers.

### 3. Docker Network
- **Current Issue:** The project is not using Docker Network to manage the containers.
- **Objective:** The project should be using Docker Network to manage the containers.
- **Action:** Use Docker Network to manage the containers.

### 4. Docker Performance
- **Objective:** In order to reduce build and deployment time, the project should be optimized for Docker using the best docker practices, such us multi-stage builds, cache layers, alpine images, lightweight dependencies, etc.
- **Action:** Optimize the project for Docker using the best docker practices.