FRONTEND_PORT=3001
# Backend API origin (absolute URL with https://). Used at container start (REGCHECK_API_BASE_URL)
# and optionally at frontend build time (VITE_API_BASE_URL).
REGCHECK_API_BASE_URL=https://your-backend.up.railway.app
VITE_API_BASE_URL=https://your-backend.up.railway.app
