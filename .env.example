FRONTEND_PORT=3001
# Backend API origin — must be absolute in production (include https://).
# Railway example: https://regcheck-backend.up.railway.app
# Local Docker backend: http://localhost:8000
# Local Vite dev (optional): leave empty to proxy /api to localhost:8000
VITE_API_BASE_URL=http://localhost:8000
