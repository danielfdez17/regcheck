FRONTEND_PORT=3001
BACKEND_PORT=8000
# Local dev + vite preview: browser → backend on host
VITE_API_BASE_URL=http://localhost:8000
# Docker make up: nginx proxy target (localhost rewritten to host.docker.internal in container)
REGCHECK_API_BASE_URL=http://localhost:8000
